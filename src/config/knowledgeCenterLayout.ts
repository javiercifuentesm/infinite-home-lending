/**
 * Knowledge Center — Connected Tools layout toggle
 *
 * `true`  → Navy card bridge (`KnowledgeConnectedTools.tsx`)
 * `false` → Legacy inline-link paragraphs (`KnowledgeConnectedToolsLegacy.tsx`)
 *
 * To revert this upgrade: set to `false` and save.
 */
export const KNOWLEDGE_CONNECTED_TOOLS_V2 = false;
