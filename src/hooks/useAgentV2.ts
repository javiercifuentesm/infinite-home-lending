/**
 * Agent V2 shell + state. Conversational decisions live in `lib/agent-v2/agentV2ResponseEngine.ts`
 * (mock orchestrator wires `generateNextBestMove` today).
 */
export { useAgentV2 } from "../context/AgentV2Context";
