/**
 * High-impact line stack — loss, present tense, one idea per line.
 */

export type SharpBlock = {
  /** 💣 Immediate impact */
  hit: string;
  /** ⚠️ Present tense */
  reality: string;
  /** 🧠 Simple truth */
  interpretation: string;
  /** 🚨 Irreversibility */
  escalation: string;
  /** 🎯 Shareable */
  micDrop: string;
};
